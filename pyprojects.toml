[project]
name = "hospital-simulation"
version = "0.1.0"
description = "Discrete Event Simulation of a Hospital Emergency Department"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "simpy>=4.1",
    "numpy",
    "pandas",
    "matplotlib",
    "scipy",
]
